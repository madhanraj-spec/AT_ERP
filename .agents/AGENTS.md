# Workspace Agent Rules

## Scratch File Hygiene
- **Automatic Deletion of Scratch Files:** Any temporary scratch file, test script, log output, or database query script created during development (whether inside a `scratch/` folder or at the workspace root) **must be deleted automatically** as soon as its immediate usage or task is completed. Do not leave any temporary files or debug artifacts behind in the repository.
